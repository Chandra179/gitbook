# API

HTTP, gRPC

* only fetch data what you need,
* beware of null, empty data
* rate limit to prevent bruteforce
* best way sending file/image on HTTP
* authorization for controlling endpoint access
* use serialization for smaller size for outgoing data
* idempotent request
* clarity and documentation for every usecase will be very helpful
* use HTTP status correctly
* retrying request

