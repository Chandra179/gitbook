# Oauth2 & Oidc

authorization URL redirection (pcke, nonce)

callback handler

refresh token

session mangement: in memory cache for now, and use http only cookie & samesite lax mode for strict for session id

logout

middleware: for validating user session

